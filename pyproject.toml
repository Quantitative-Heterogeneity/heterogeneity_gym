[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
authors = [
   { name = "Erik Thiede", email = "ehthiede@gmail.com"},
]
name = "heterogeneity_gym"
version = "0.0.1"
dependencies = [
    "numpy",
]
